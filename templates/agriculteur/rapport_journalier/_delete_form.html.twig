<form method="post" action="{{ path('app_rapport_journalier_delete', {'idRapport': rapport_journalier.idRapport}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ rapport_journalier.idRapport) }}">
    <button class="btn">Delete</button>
</form>
