knp_snappy:
  pdf:
    enabled: true
    binary: '%env(string:WKHTMLTOPDF_BINARY)%'
    options:
      encoding: 'UTF-8'
      enable-local-file-access: true

  image:
    enabled: true
    binary: '%env(string:WKHTMLTOIMAGE_BINARY)%'
    options: { }